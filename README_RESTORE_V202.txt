Restore v202

This restores app/globals.css back to the version from the uploaded sports-tracker(13).zip before the v201 layout override pass.
It also gives you a safe command below to remove any accidental root-level globals.css file from v200 if it was copied into the repo.
