GameTracker v130 patch

Focused CSS-only polish from the v129 base:
- Uses the main Scores page pitcher/meta typography on GameTracker Scoring and Plays text.
- Uses the same meta styling on the Plays pitcher tag.
- Fixes top clipping on final-game score numbers in the big GameTracker header.
- Changes final winner/loser score colors/weights to match the main Scores page winner/loser treatment instead of the stronger accent pop.
